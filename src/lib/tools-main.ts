import {
  // getCurrentTime,
  // calculateBMI,
  // getWeather,
  searchWeb,
  // askForConfirmation,
} from "./tools";

export const tools = {
  // getCurrentTime,
  // calculateBMI,
  // getWeather,
  searchWeb,
  // askForConfirmation,
};
