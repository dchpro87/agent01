import {
  // getCurrentTime,
  // calculateBMI,
  // getWeather,
  searchWeb,
  // askForConfirmation,
  doocumentSummarizer,
} from "./tools";

export const tools = {
  // getCurrentTime,
  // calculateBMI,
  // getWeather,
  searchWeb,
  doocumentSummarizer,
  // askForConfirmation,
};
