import {
  // askForConfirmation,
  // calculateBMI,
  documentSummarizer,
  searchWeb,
} from "./tools";

export const tools = {
  // askForConfirmation,
  // calculateBMI,
  documentSummarizer,
  searchWeb,
};
